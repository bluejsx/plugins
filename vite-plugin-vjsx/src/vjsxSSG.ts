//compile codes for the production build
function compileForSSG(code: string): string {
  
  return code
}

//compile JS/TS code for building 
function compileForSSGRender(code: string): string{

  return code
}


export { compileForSSG, compileForSSGRender }